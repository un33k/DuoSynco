#!/bin/bash
# Ali - AI Line Interpreter (minimal bash wrapper)

# Get project root directory
PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$PROJECT_DIR"

# Execute Python CLI module
exec python -m ali.cli "$@"